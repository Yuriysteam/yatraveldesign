name: Publish prototypes

on:
  push:
    branches: [main]
    paths:
      - 'assets/**'
      - 'prototypes/**'
      - 'skills/**'
      - 'index.html'
      - 'prototypes.json'
      - 'robots.txt'
      - '.nojekyll'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Build static site
        run: |
          mkdir public
          cp -R assets prototypes skills public/
          cp index.html prototypes.json robots.txt .nojekyll public/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public
      - id: deployment
        uses: actions/deploy-pages@v4
